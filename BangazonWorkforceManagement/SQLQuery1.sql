SELECT t.Id,
                                        t.Name,
                                        t.StartDate,
                                        t.EndDate,
                                        t.MaxAttendees
                                    FROM TrainingProgram t